;; Layer-rack sampler panel regression fixture. The sample is intentionally a
;; checked-in audio asset so the production capture remains self-contained.
(capture-project
  (track :layer-rack
    :name "Layer Rack"
    :samples ("../assets/ir/lexicon-300-rich-plate.wav")))
