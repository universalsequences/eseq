;; Production-path capture of the Filter Table response editor (eseq-dtx.8):
;; open session with one parametric node, seeded tilt, frame 12 selected.
(capture-project
  (track :sampler
    :name "Filter Table"
    :audio-fx ("Filter Table")
    :filter-table-editor true))
