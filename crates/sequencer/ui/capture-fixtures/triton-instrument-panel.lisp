;; Triton saved-instrument UI capture. The instrument is compiled and initialized
;; through the same host path used by an interactively added track.
(capture-project
  (track :instrument "core/triton"))
