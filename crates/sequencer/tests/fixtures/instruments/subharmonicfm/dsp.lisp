;; generated by the patch editor — edit via the patcher or eject to code

(def mod1 (in 6 @name mod1 @modulator 1))
(def mod2 (in 7 @name mod2 @modulator 2))
(def mod3 (in 8 @name mod3 @modulator 3))
(def mod4 (in 9 @name mod4 @modulator 4))

(defmacro spatial-quad-filter (in1 in2 in3 in4 cutoff q spatial_width)
  (def f1 (svf in1 cutoff q 0))
  (def mul-0 (* cutoff 1.4))
  (def f2 (svf in2 mul-0 q 1))
  (def mul-1 (* cutoff 1.9))
  (def f3 (svf in3 mul-1 q 2))
  (def mul-10 (* f1 0.1))
  (def mul-2 (* cutoff 0.7))
  (def f4 (svf in4 mul-2 q 1))
  (def mul-3 (* spatial_width 320))
  (def d2 (delay f2 mul-3))
  (def mul-11 (* d2 0.2))
  (def mul-4 (* spatial_width 580))
  (def d3 (delay f3 mul-4))
  (def mul-12 (* d3 0.5))
  (def mul-5 (* spatial_width 840))
  (def d4 (delay f4 mul-5))
  (def mul-13 (* d4 0.8))
  (def mul-6 (* f1 0.8))
  (def mul-7 (* d2 0.5))
  (def mul-8 (* d3 0.2))
  (def mul-9 (* d4 0.1))
  (def out_l (+ mul-6 mul-7 mul-8 mul-9))
  (def out_r (+ mul-10 mul-11 mul-12 mul-13))
  (tuple out_l out_r))

(defmacro subharmonic-sines (freq)
  (def fm_depth (param fmdepth @default 0 @min 0 @max 2000 @mod true @mod-mode additive))
  (def fm_ratio (param fmratio @default 1 @min 0.1 @max 8))
  (def f2 (/ freq 2))
  (def f3 (* freq 1.344))
  (def f4 (/ freq 4))
  (def mul-1 (* freq fm_ratio))
  (def n0-1 0.1)
  (def n0-1-1 0.1)
  (def n0-1-2 0.1)
  (def n0-1-3 0.1)
  (def phasor-0 (phasor mul-1))
  (def mul-0 (* phasor-0 twopi))
  (def sin-0 (sin mul-0))
  (def mod_sig (* sin-0 fm_depth))
  (def add-0 (+ freq mod_sig))
  (def div-0 (/ mod_sig 2))
  (def add-1 (+ f2 div-0))
  (def div-1 (/ mod_sig 3))
  (def add-2 (+ f3 div-1))
  (def div-2 (/ mod_sig 4))
  (def add-3 (+ f4 div-2))
  (def max-0 (max n0-1 add-0))
  (def max-1 (max n0-1-1 add-1))
  (def max-2 (max n0-1-2 add-2))
  (def max-3 (max n0-1-3 add-3))
  (def phasor-1 (phasor max-0))
  (def mul-2 (* phasor-1 twopi))
  (def phasor-2 (phasor max-1))
  (def mul-3 (* phasor-2 twopi))
  (def phasor-3 (phasor max-2))
  (def mul-4 (* phasor-3 twopi))
  (def phasor-4 (phasor max-3))
  (def mul-5 (* phasor-4 twopi))
  (def s1 (sin mul-2))
  (def s2 (sin mul-3))
  (def s3 (sin mul-4))
  (def s4 (sin mul-5))
  (tuple s1 s2 s3 s4))

(param attack @group amp @env amp-env @role attack @default 5 @min 0 @max 1000 @unit ms)
(param decay @group amp @env amp-env @role decay @default 120 @min 1 @max 2000 @unit ms)
(param gain @default 0.5 @min 0 @max 1 @mod true @mod-mode additive)
(param release @group amp @env amp-env @role release @default 180 @min 1 @max 5000 @unit ms)
(param sustain @group amp @env amp-env @role sustain @default 0.8 @min 0 @max 1)

(def gate (in 1 @name gate))
(def pitch (in 2 @name pitch))
(def velocity (in 3 @name velocity))
(def trigger (in 4 @name trigger))
(def clock (in 5 @name clock))

(def env (adsr gate trigger attack decay sustain release))
(def (out-2 out2 out3 out4) (subharmonic-sines pitch))
(def mul (* out-2 0.5))
(def phase (phasor pitch))
(def value 3000)
(def value-2 9)
(def value-3 0.1)
(def (out-3 out2-2) (spatial-quad-filter mul out2 out3 out4 value value-2 value-3))
(def mul-0 (* out-3 env velocity (mod gain)))
(def mul-2 (* out2-2 env velocity (mod gain)))

(out mul-0 1 @name audio)
(out mul-2 2 @name audio-2)
